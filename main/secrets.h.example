#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"
#define CLAUDE_TOKEN  "your-claude-oauth-access-token"

/* Optional: require an "X-Auth: <secret>" header on the mutating endpoints
 * (POST / token update and POST /ota firmware upload). Leave commented out to
 * keep them open (legacy behavior). When set, push_claude_token.py must send
 * the same secret via --secret / CLAUDE_METER_SECRET. */
// #define CFG_AUTH_SECRET "choose-a-long-random-string"
